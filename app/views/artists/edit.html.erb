<%= render partial: 'form', locals: {
                                      url: "/artists/#{@artist.id}",
                                      method: :patch,
                                      button_text: 'Update Artist'
                                    } %>
